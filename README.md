# Bake Me A Byte

very bad and basic bytebeat player. 

~~it uses AudioContexts, which iirc are almost obsolete.~~

it only displays errors in the console too.

anddd, it probably can run arbitrary code.

~~also, it doesn't generate a link for the code, so you have to manually copy and paste.~~ this was added 24-05-26

### no, i won't add a library.